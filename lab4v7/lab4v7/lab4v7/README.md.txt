# Лабораторна робота №4  
## Варіант 7 — Сенсори  
### Дисципліна: Об’єктно-орієнтоване програмування

---

## 📌 Тема роботи
Інтерфейси, абстрактні класи, композиція та агрегація на прикладі системи сенсорів.

---

## 🎯 Мета роботи
1. Навчитися створювати інтерфейси та абстрактні класи.
2. Реалізувати кілька конкретних класів на основі спільного абстрактного предка.
3. Використати композицію / агрегацію між класами.
4. Навчитися працювати зі списками вимірювань та обробкою даних.
5. Виявляти відхилення від норми для останнього вимірювання.

---

## 📝 Опис виконання завдання

### ✔ 1. Створено інтерфейс **ISensor**
Інтерфейс описує спільний функціонал сенсорів:

- `string Name { get; }`
- `void AddReading(double value)`
- `IReadOnlyList<double> GetReadings()`
- `double GetAverage()`
- `bool IsLastValueOutOfRange()`

Цей інтерфейс гарантує єдиний контракт для всіх типів сенсорів.

---

### ✔ 2. Створено абстрактний клас **SensorBase**
Абстрактний клас містить спільну логіку для всіх сенсорів:

- зберігання вимірювань у `List<double>`
- обчислення середнього значення
- перевірка, чи останнє значення виходить за межі норми
- абстрактні властивості:
  - `NormalMin`
  - `NormalMax`

Кожен конкретний сенсор визначає власні норми.

---

### ✔ 3. Реалізовано два сенсори:

#### 🔹 TemperatureSensor
- Межі норми: **18–26 °C**
- Призначення: вимірювання температури

#### 🔹 PressureSensor
- Межі норми: **740–760 мм рт. ст.**
- Призначення: вимірювання атмосферного тиску

Обидва класи успадковуються від `SensorBase` та перевизначають `NormalMin`/`NormalMax`.

---

### ✔ 4. Створено клас **MonitoringSystem**
Цей клас виконує агрегацію сенсорів:

- містить список `List<ISensor>`
- метод `AddSensor(ISensor sensor)` додає сенсор у систему
- метод `PrintReport()` виводить інформацію про всі сенсори:
  - вимірювання
  - середнє значення
  - повідомлення про відхилення від норми

Це демонструє агрегацію: система **має** сенсори, але не створює їх сама.



